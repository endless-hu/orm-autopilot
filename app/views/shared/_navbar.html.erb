<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="/orms">ORM Autopilot</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'New', new_orm_path, :class => 'nav-link active' %>
        </li>
        <li class="nav-item">
          <%= link_to 'History', orms_path, :class => 'nav-link'  %>
        </li>
      </ul>
    </div>
    <!-- right align the link -->
    <% if @current_user %>
    <div class="text-end">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Update User Info', edit_user_path(@current_user), :class => 'nav-link' %>
        </li>
        <li>
          <%= button_to "Logout", logout_path, method: :delete, class: "relative w-full inline-flex justify-center text-center px-3 py-1 rounded-l-md border border-gray-300 bg-white text-sm font-medium text-gray-700 hover:bg-gray-50 focus:outline-none" %>
        </li>
      </ul>
    </div>
    <% end %>
  </div>
</nav>